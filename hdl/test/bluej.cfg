adapter driver remote_bitbang

if {![info exists BLUEJ_JTAG_SOCKET]} {
    set BLUEJ_JTAG_SOCKET /tmp/jtag.sock
}

remote_bitbang port 0
remote_bitbang host $BLUEJ_JTAG_SOCKET

transport select jtag
# poll off
jtag newtap bluej tap -irlen 8 -ircapture 0x01 -irmask 0xff

scan_chain

init
